import React from "react";
import styled from "styled-components";

const StyledStartUpCard = styled.div``;

const StartUpCard = ({
  ProjectName,
  ProjectDescription,
  ProjectLogo,
  CurrentFunding,
  FundingGoal,
  GiveLink,
  LearnLink,
}) => {
  return <StyledStartUpCard></StyledStartUpCard>;
};

export default StartUpCard;
